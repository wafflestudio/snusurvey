
<div class="table">
  <table class="listing form" cellpadding="0" cellspacing="0">
    <tr>
			<th colspan="2">Reply</th>
		</tr>
					<tr>
				<td><strong>created_at</strong></td>
				<td><%= @reply.created_at %></td>
			</tr>
					<tr>
				<td><strong>updated_at</strong></td>
				<td><%= @reply.updated_at %></td>
			</tr>
					<tr>
				<td><strong>question_id</strong></td>
				<td><%= @reply.question_id %></td>
			</tr>
					<tr>
				<td><strong>paper_id</strong></td>
				<td><%= @reply.paper_id %></td>
			</tr>
					<tr>
				<td><strong>example_ids</strong></td>
				<td><%= @reply.example_ids %></td>
			</tr>
			</table>
</div>

<div class="actions">
	<%= link_to 'List', admin_replies_path, :class => "action list" %> |
	<%= link_to 'Edit', edit_admin_reply_path(@reply), :class => "action edit" %> |
	<%= link_to 'Destroy', admin_reply_path(@reply), :method => :delete, :confirm => "Are you sure?", :class => "action destroy" %>
</div>
