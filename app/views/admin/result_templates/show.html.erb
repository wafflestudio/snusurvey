<div class="top-bar">
	<h1><%= @result_template.title%></h1>
</div>

<div class="table">
  <table class="listing form" cellpadding="0" cellspacing="0">
    <tr>
			<th colspan="2">ResultTemplate</th>
		</tr>
					<tr>
				<td><strong>created_at</strong></td>
				<td><%= @result_template.created_at %></td>
			</tr>
					<tr>
				<td><strong>updated_at</strong></td>
				<td><%= @result_template.updated_at %></td>
			</tr>
					<tr>
				<td><strong>title</strong></td>
				<td><%= @result_template.title %></td>
			</tr>
					<tr>
				<td><strong>description</strong></td>
				<td><%= @result_template.description %></td>
			</tr>
					<tr>
				<td><strong>type</strong></td>
				<td><%= @result_template.type %></td>
			</tr>
			</table>
</div>

<div class="actions">
	<%= link_to 'List', admin_result_templates_path, :class => "action list" %> |
	<%= link_to 'Edit', edit_admin_result_template_path(@result_template), :class => "action edit" %> |
	<%= link_to 'Destroy', admin_result_template_path(@result_template), :method => :delete, :confirm => "Are you sure?", :class => "action destroy" %>
</div>
