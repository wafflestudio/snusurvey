<div class="top-bar">
	<h1><%= @survey.title%></h1>
</div>

<div class="table">
  <table class="listing form" cellpadding="0" cellspacing="0">
    <tr>
			<th colspan="2">Survey</th>
		</tr>
					<tr>
				<td><strong>created_at</strong></td>
				<td><%= @survey.created_at %></td>
			</tr>
					<tr>
				<td><strong>updated_at</strong></td>
				<td><%= @survey.updated_at %></td>
			</tr>
					<tr>
				<td><strong>title</strong></td>
				<td><%= @survey.title %></td>
			</tr>
					<tr>
				<td><strong>description</strong></td>
				<td><%= @survey.description %></td>
			</tr>
					<tr>
				<td><strong>template_id</strong></td>
				<td><%= @survey.template_id %></td>
			</tr>
					<tr>
				<td><strong>result_template_id</strong></td>
				<td><%= @survey.result_template_id %></td>
			</tr>
					<tr>
				<td><strong>admin_id</strong></td>
				<td><%= @survey.admin_id %></td>
			</tr>
			</table>
</div>

<div class="actions">
	<%= link_to 'List', admin_surveys_path, :class => "action list" %> |
	<%= link_to 'Edit', edit_admin_survey_path(@survey), :class => "action edit" %> |
	<%= link_to 'Destroy', admin_survey_path(@survey), :method => :delete, :confirm => "Are you sure?", :class => "action destroy" %>
</div>
