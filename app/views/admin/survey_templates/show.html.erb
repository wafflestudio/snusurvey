<div class="top-bar">
	<h1><%= @survey_template.title%></h1>
</div>

<div class="table">
  <table class="listing form" cellpadding="0" cellspacing="0">
    <tr>
			<th colspan="2">SurveyTemplate</th>
		</tr>
					<tr>
				<td><strong>created_at</strong></td>
				<td><%= @survey_template.created_at %></td>
			</tr>
					<tr>
				<td><strong>updated_at</strong></td>
				<td><%= @survey_template.updated_at %></td>
			</tr>
					<tr>
				<td><strong>title</strong></td>
				<td><%= @survey_template.title %></td>
			</tr>
					<tr>
				<td><strong>description</strong></td>
				<td><%= @survey_template.description %></td>
			</tr>
					<tr>
				<td><strong>public</strong></td>
				<td><%= @survey_template.public %></td>
			</tr>
					<tr>
				<td><strong>perpage</strong></td>
				<td><%= @survey_template.perpage %></td>
			</tr>
			</table>
</div>

<div class="actions">
	<%= link_to 'List', admin_survey_templates_path, :class => "action list" %> |
	<%= link_to 'Edit', edit_admin_survey_template_path(@survey_template), :class => "action edit" %> |
	<%= link_to 'Destroy', admin_survey_template_path(@survey_template), :method => :delete, :confirm => "Are you sure?", :class => "action destroy" %>
</div>
