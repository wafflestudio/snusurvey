
<div class="table">
  <table class="listing form" cellpadding="0" cellspacing="0">
    <tr>
			<th colspan="2">Feedback</th>
		</tr>
					<tr>
				<td><strong>created_at</strong></td>
				<td><%= @feedback.created_at %></td>
			</tr>
					<tr>
				<td><strong>content</strong></td>
				<td><%= @feedback.content %></td>
			</tr>
					<tr>
				<td><strong>password</strong></td>
				<td><%= @feedback.password %></td>
			</tr>
					<tr>
				<td><strong>users_id</strong></td>
				<td><%= @feedback.users_id %></td>
			</tr>
			</table>
</div>

<div class="actions">
	<%= link_to 'List', admin_feedbacks_path, :class => "action list" %> |
	<%= link_to 'Edit', edit_admin_feedback_path(@feedback), :class => "action edit" %> |
	<%= link_to 'Destroy', admin_feedback_path(@feedback), :method => :delete, :confirm => "Are you sure?", :class => "action destroy" %>
</div>
