<div class="table">
	<table class="listing" cellpadding="0" cellspacing="0">
		<tr>
					</tr>
		<% @categories.each do |category| -%>
			<tr>
							</tr>
    <% end %>
		<% if @categories.empty? %>
      <tr>
				<td colspan="0">No records</td>
      </tr>
    <% end %>
  </table>
</div>

<div class="actions">
	<a href="<%= new_admin_category_path %>" class="action add">New Category</a>
</div>
