Title: <%= @survey.title %>
Description: <%= @survey.description %>
Survey Template: <%= @survey.survey_template.title %>
Result Template: <%= @survey.result_template.title %>

</div>
<%= form_for(@survey) do |f| %>
   <div id="questions_of_survey">
      <%= link_to "Add Question", new_question_path(:survey_id => @survey._id, :question_template_type => 0), :remote => true %>
      <%= f.fields_for :questions do |question| %>
      <% end %>
   </div>
   <%= f.submit %>
<% end %>
