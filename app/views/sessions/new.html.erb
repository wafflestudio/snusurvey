<%= link_to "auth", flash[:msg].html_safe %>
<%= form_for(:sessions, :url => signin_path, :method => "post") do |f| %>
   <%= f.label :name %>
   <%= f.text_field :name %>
   <%= f.label :password %>
   <%= f.password_field :password %>
   <%= f.submit %>
<% end %>
